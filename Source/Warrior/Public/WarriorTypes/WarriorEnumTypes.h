#pragma once

//enum needed for multiple exection pins:
UENUM()
enum class EWarriorConfirmType : uint8
{
	Yes,
	No
};

//enum needed for multiple exection pins:
UENUM()
enum class EWarriorValidType: uint8
{
	Valid,
	Invalid
};

//enum needed for multiple exection pins:
UENUM()
enum class EWarriorSuccessType : uint8
{
	Successful,
	Failed
};
